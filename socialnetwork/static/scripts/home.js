/**
 * @description: This function is called when the page is loaded
 */
const onLoad = () => {
    //honestly I think I had plans for this. not sure what for.
    //We'll just leave it here incase I remember what I was going to do with it. 
    return;
};

window.addEventListener('load', onLoad);